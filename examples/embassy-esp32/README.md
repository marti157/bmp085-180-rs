## ESP32 with Embassy example

Refer to [ESP-rs book, Xtensa Target](https://docs.esp-rs.org/book/installation/riscv-and-xtensa.html), as the `esp` toolchain needs to be installed.

`$ cargo run --release`
